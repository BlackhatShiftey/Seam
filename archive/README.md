# SEAM Archive

This folder is for inactive project material.

Archived files are retained for traceability, not as current instructions or
runtime source. Agents should not read this folder during normal startup or
feature work. Use it only when investigating historical decisions, old handoffs,
or deliberately retired code.

Current working areas:

- active runtime code: `../seam_runtime/` and `../seam.py`
- active experiments: `../experimental/`
- active tooling: `../tools/`, `../scripts/`, and `../installers/`
- active docs: `../docs/`
- inactive docs: `../docs/archive/`
- inactive code: `code/`
