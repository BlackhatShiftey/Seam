# Maintenance Ledger

Maintenance records cover local service state, cleanup, ports, and recurring
operator actions.

Routes:

- `maintenance/docker`
- `maintenance/pgvector`

Use `tools.history.build_context_pack --route maintenance/docker` for bounded
Docker maintenance history.
