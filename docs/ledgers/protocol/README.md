# Protocol Ledger

Protocol ledgers describe how agents preserve continuity without loading
unbounded history.

Routes:

- `protocol/context`
- `protocol/security`

The canonical agent protocol remains `AGENTS.md`.
