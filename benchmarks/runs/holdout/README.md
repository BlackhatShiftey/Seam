# SEAM Holdout Runs

Default `seam benchmark run --holdout` bundles are written here when no `--output` is provided.

JSON result bundles in this directory are ignored by git. Publish them intentionally through the benchmark publication flow, not as routine development artifacts.
